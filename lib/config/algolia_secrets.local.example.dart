/// Copy to `algolia_secrets.local.dart` and add your Algolia **search-only** key.
/// Never put the admin key here — that stays in Firebase Functions config only.
const String algoliaAppId = '';
const String algoliaSearchKey = '';
